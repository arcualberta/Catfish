@model Catfish.Core.Models.Contents.Fields.TextArea

@for (int i = 0; i < Model.Values.Count; ++i)
    @Html.DisplayFor(model => Model.Values[i], "Values/" + Model.Values[i].GetType().ToString(), null, new { multiline = true })
