@model Catfish.Models.Blocks.JavascriptBlock

@{ 
    bool containScriptTag = Model.GetJavascript() == "" ? false : Model.GetJavascript().Contains("<script>") ? true : false;
}

@if (containScriptTag)
{
    Html.Raw(Model.GetJavascript());
}
else {

    <script>
        @Html.Raw(Model.GetJavascript())
    </script>
}