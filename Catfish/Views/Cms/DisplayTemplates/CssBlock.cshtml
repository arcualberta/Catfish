@model Catfish.Models.Blocks.CssBlock

@{
    bool containStyleTag = Model.GetCss() == "" ? false : Model.GetCss().Contains("<style>") ? true : false;
}

@if (containStyleTag)
{
    Html.Raw(Model.GetCss());
}
else
{

    <style>
        @Html.Raw(Model.GetCss())
    </style>
}