@model Catfish.Core.Models.ViewModels.GroupRoleAssignmentVM

@inject Piranha.Manager.ManagerLocalizer Localizer
@inject Catfish.Core.Services.GroupService UserGroupService
    
<div class="form-group">
    <label class="title">@Localizer.General[Model.RoleName]</label>
    <div class="container-fluid">
        <table class="table table-borderless">
            <thead>
                <tr>
                    <th>@Localizer.General["User Name"]</th>
                    <th>@Localizer.General["Email"]</th>
                    <th class="actions one"></th>
                </tr>
            </thead>
            <tbody>
                @{ 
                    //TODO: initialize the offset and max using variables passed through the page model.
                    int offset = 0;
                    int max = 10000;
                }
                @foreach (var user in UserGroupService.GetGroupRoleUser(Model.RoleGroupId))
                 {
                    <tr>
                        <td>
                            <span class="title">@user.User.UserName</span>
                        </td>
                        <td>
                            <span class="title">@user.User.Email</span>
                        </td>
                        <td class="actions one">
                            <form method="delete">
                                <a class="danger" href="@Url.Action("Delete", new { id = user.Id })">
                                    <span class="fas fa-trash"></span>
                                </a>

                            </form>
                            @*<form  method="post" asp-route-id="@user.Id" asp-controllr="Group">
                                <button action="Delete" type ="submit" class="btn btn-danger"><i class="fas fa-trash"></i></button>
                            </form>*@

                        </td>
                    </tr>
                 }
            </tbody>
        </table>
    </div> 
    <div class="float-right">
        <a class="btn btn-labeled btn-primary pull-right" href="@string.Format("user/{0}", Model.RoleGroupId)">  @Localizer.General["Add User"]</a>
    </div>
    <br>
</div>