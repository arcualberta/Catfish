@model Catfish.Core.Models.Contents.Fields.TextField



@foreach (var val in Model.Values)
{
    foreach (var txt in val.Values)
    {
        <div>@txt.Language</div>
        <div>@txt.Value</div>
    }

}