@model Catfish.Core.Models.Metadata.TextField

@Html.Hidden("ModelType", Model.GetType().AssemblyQualifiedName, new Dictionary<string, object>()
{
    {"class", "model-type"}
})
@Html.HiddenFor(m => m.Ref)

<div>
    @Html.Label(Model.Name)
    @Html.EditorFor(m => m.Value)
</div>

